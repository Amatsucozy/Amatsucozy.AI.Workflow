<!--
Template for sonarqube-issues/resources/projects/<org-lowercase>/map.md,
owned by the sonarqube-project-map skill (the producer of that file).

This is the canonical structure for every per-org project map. This
skill generates/refreshes map.md files by copying this structure
verbatim and filling in the placeholders below — it must not rename,
reorder, add, or remove section headers, so that every org's map.md is
diffable against the last run and against every other org's map.md.
Free-text only goes inside table cells, bullet lists, and the
placeholders marked <...>; everything else (headers, table column order,
prose outside placeholders) is fixed.

Placeholders:
  <ORG>            GitHub org name, as-cased on GitHub (e.g. "Acme-Corp")
  <ORG_LOWER>      <ORG> lowercased (matches the containing directory name)
  <ORG_URL>        GitHub org URL
  <DATE>           ISO date (YYYY-MM-DD) this file was (re)generated
-->

# SonarQube ↔ GitHub Project Map — `<ORG_LOWER>`

- GitHub org: `<ORG>` (<ORG_URL>)
- SonarQube account: the org-level SonarQube instance behind the
  `sonarqube` MCP server used in this workspace
- Last generated: <DATE>
- Generated by: the `sonarqube-project-map` skill — re-run it to refresh
  this file rather than hand-editing the tables below.

## Mapped projects

<!-- One row per confidently-matched SonarQube project. Keep the column
order fixed: SonarQube key, GitHub repo, Notes (Notes may be empty). -->

| SonarQube key | GitHub repo | Notes |
|---|---|---|
| `<SONARQUBE_KEY>` | `<ORG>/<REPO>` | |

## GitHub repos with no SonarQube project (as of last generation)

<!-- One bullet per GitHub repo in the org that has no SonarQube project
at all. If none, write "None — every repo in the org has a SonarQube
project." instead of leaving this empty. -->

- `<REPO_NAME>`

## Unmapped — verify manually

<!-- One bullet per SonarQube project or GitHub repo that could not be
confidently matched to a counterpart on the other side, each with a
one-line reason (e.g. "no GitHub repo with a similar name found" or
"two equally-plausible GitHub repos, needs a human decision"). This
section must always be present, even when empty, so it's clear the check
ran. If empty, write exactly:
"None as of last generation — every SonarQube project matched exactly
one GitHub repo." -->

None as of last generation — every SonarQube project matched exactly one
GitHub repo.

## Regenerating this file

Invoke the `sonarqube-project-map` skill with org `<ORG>` (or just ask to
"refresh the sonarqube project map") to re-query GitHub `search_repositories`
and SonarQube `search_my_sonarqube_projects` and rewrite the tables above.
