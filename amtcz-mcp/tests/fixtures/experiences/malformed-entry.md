---
slug: malformed-entry
use-when: This entry is intentionally malformed for testing (no closing frontmatter delimiter)

Body text without ever closing the frontmatter block above with a second `---`.
